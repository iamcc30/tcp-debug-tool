# TCP调试助手使用手册

适用版本：**1.3.0**。本手册以中文界面为例，英文界面的操作流程相同。

[官方网站](https://tcp.onemoretry.cc/) · [下载与发布说明](https://github.com/iamcc30/tcp-debug-tool/releases/tag/v1.3.0) · [Mac App Store](https://apps.apple.com/cn/app/tcp%E8%B0%83%E8%AF%95%E5%8A%A9%E6%89%8B/id6808118556?mt=12)

## 1. 安装与启动

| 平台 | 获取方式 | 启动说明 |
| --- | --- | --- |
| macOS Intel / Apple Silicon | Mac App Store | 由商店安装和更新，一次购买，价格以商店页面为准 |
| Windows x64 | GitHub Release 的 `TCP-Debug-Windows-amd64.exe` | 下载后运行；当前发行版未进行代码签名 |
| Linux x64 / ARM64 | GitHub Release 对应架构的可执行文件 | 添加执行权限；需要兼容 Debian 12 的桌面及 OpenGL / X11 运行库 |

Linux x64 示例：

```sh
chmod +x TCP-Debug-Linux-amd64
./TCP-Debug-Linux-amd64
```

发行页附有 `SHA256SUMS.txt`。Mac 不需要下载 GitHub 上的旧 DMG；当前 Windows/Linux 发行范围不包括 x86 和 Windows ARM64。

## 2. 先完成一次本机收发

无需额外设备，可以用同一个应用中的服务端与客户端验证操作。

1. 新建连接，类型选择 **TCP 服务端**，本地地址填 `127.0.0.1`，端口填 `18080`；首次练习保持分帧关闭。
2. 点击 **保存并监听**，确认服务端管理窗口显示正在监听。如果端口被占用，换一个空闲端口。
3. 再新建一个 **TCP 客户端**配置，目标同样为 `127.0.0.1:18080`，点击 **保存并连接**。
4. 在客户端会话选择 **文本 (ASCII)**，输入 `hello`，点击 **发送**。
5. 服务端管理窗口出现接入客户端；点击 **打开会话**，查看收到的 `hello`。
6. 在服务端的该会话中输入 `world` 并发送，回到客户端查看响应。
7. 完成后停止监听。该服务端下的接入连接会断开。

应用不会自动把收到的内容回显；第 6 步需要手动回复。

![客户端与服务端连接管理](https://tcp.onemoretry.cc/images/01-overview-1.3.png)

*截图为 macOS 真实界面展示；示例端口可按本机环境调整。*

## 3. 作为 TCP 客户端连接设备

1. 点击新建连接，选择 TCP 客户端。
2. 填写名称、设备地址和端口；按需要设置连接超时（1–60 秒）。
3. **保存配置**只保存参数，适合设备暂时离线时使用；**保存并连接**会立即尝试连接。
4. 从已保存的配置建立连接或新会话，在对应窗口中收发数据。

一个配置可以有多个会话。判断当前连接时，应查看会话的远端地址、状态与字节统计。断开后需要手动重新建立连接，当前版本没有自动重连功能。

## 4. 作为 TCP 服务端接收连接

监听地址决定哪些设备可以连接：

| 本地地址 | 含义 |
| --- | --- |
| `127.0.0.1` | 仅接受当前电脑上的 IPv4 客户端，适合本机联调 |
| 当前电脑的局域网 IP | 在指定网卡地址上监听，客户端使用此地址连接 |
| `0.0.0.0` | 监听全部本地 IPv4 接口；客户端应填写服务器实际 IP，而不是 `0.0.0.0` |
| `::1` / `::` | IPv6 本机回环 / 全部本地 IPv6 接口 |

- **保存配置**不会开始监听；使用 **保存并监听**，或在服务端管理窗口手动开始监听。
- 每个接入客户端都有独立会话，可分别打开、发送、查看报文和断开。当前版本不支持广播发送。
- **停止监听**或关闭服务端管理窗口，会断开该监听器下的客户端，不影响其他监听器或主动客户端连接。
- 编辑或替换正在运行的服务端配置会停止旧监听；保存后需重新开始监听。
- 程序启动时不会自动监听。重启监听后，远端设备需要重新连接。

在局域网联调时，确认两端网络互通，并允许应用访问相应网络。Mac 首次访问局域网时，应按用途允许系统网络访问提示。

![多客户端接入与独立会话](https://tcp.onemoretry.cc/images/02-server-1.3.png)

## 5. 发送数据与切换格式

发送格式与接收显示格式可分别选择。相同字节可以按不同格式查看；切换显示格式不会改变已收到的原始数据。

| 格式 | 输入示例 | 用途 |
| --- | --- | --- |
| 文本 (ASCII) | `hello` | 文本协议或可读消息 |
| 十六进制 (Hex) | `48 65 6C 6C 6F` | 精确输入字节，以上内容对应 `Hello` |
| 二进制 (Binary) | `01000001` | 按二进制位输入字节，以上内容对应 `A` |
| JSON | `{"command":"ping"}` | JSON 文本内容 |

发送时点击 **发送**，也可以按 **Ctrl+Enter**（Windows/Linux）或 **⌘+Enter**（Mac）。HEX 输入需要完整的两位字节；Binary 每个字节需要 8 位。

- **消息历史**可复用以前发送的内容。
- **转换**用于在当前格式间处理输入；发送前再次确认输入内容和格式。
- **清空输入**只清除编辑框，不清除报文记录。
- 如果需要换行字节，可在 HEX 中明确发送 `0A` 或 `0D 0A`。不要把文本中的字面量 `\n` 当成已经发送了换行。

## 6. 按协议设置接收分帧

TCP 是字节流，一次发送不保证对应一条接收记录。关闭分帧时，接收记录按实际读取到的数据块产生；需要按协议恢复消息边界时再开启分帧。

| 模式 | 配置与行为 | 接收示例 |
| --- | --- | --- |
| 长度前缀 | 前缀大小为 **1、2、4 或 8 字节**，大端序；数值表示后续有效载荷的字节数，不包含前缀本身 | 2 字节前缀：`00 03 41 42 43` 得到 `41 42 43` |
| 分隔符 | 填写 HEX 分隔符；输出消息不包含分隔符 | 分隔符 `0D 0A`：`41 42 0D 0A` 得到 `41 42` |
| 固定长度 | 按指定字节数输出完整消息 | 长度 3：`41 42 43 44 45 46` 得到两条 3 字节消息 |

未收齐的消息会等待后续数据，不能仅凭暂时没有显示就判断连接失败。长度前缀模式不接受零长度消息，单条有效载荷上限为 10 MiB；未完成的分帧残留也有 10 MiB 限制。

这些设置用于**接收分帧**。应用不会自动为发送内容添加长度前缀或分隔符；需要按对端协议自行构造要发送的完整字节。

## 7. 查看报文与历史

- **报文列表**：逐条查看时间、TX/RX 方向、大小和内容。大报文可能只显示有界预览，完整内容通过复制或详情查看。
- **会话流**：按会话连续阅读。可独立启用纯数据视图，以及数据间自动换行。
- **纯数据**隐藏会话流中的元信息，不会修改原始报文。
- **数据间自动换行**只控制相邻报文之间是否额外补换行；报文自身的换行仍会保留。
- 上滚查看旧消息时暂停自动跟随；可点击新消息提示回到最新位置。
- 会话流使用活动窗口和历史分页；通过“更早 / 较新 / 最新”查看其他范围，当前屏幕不是全部历史。

断开或停止监听后，尚保留的会话可继续查看历史。关闭单个会话窗口会释放会话资源；磁盘数据库文件保留，但不要把这一点当成应用已提供跨重启历史浏览入口。需要留存的记录请先导出。

![报文与格式查看](https://tcp.onemoretry.cc/images/03-packets-1.3.png)

## 8. 定时发送

1. 建立连接，在输入框准备好要发送的内容和格式。
2. 点击 **定时发送**，设置正数间隔，单位为毫秒；例如 `1000` 表示每秒发送一次。
3. 启动后会重复发送启动时准备的内容。要改变内容或间隔，先停止，再重新设置。
4. 点击 **停止定时**结束；连接断开时也会停止。

定时发送运行期间，先停止定时器再手动发送，避免混淆测试数据。

## 9. 导出与清空

点击当前会话中的 **导出数据**，选择 CSV、JSON 或 TXT，再选择保存位置。导出用于保留和进一步分析当前会话的记录，不只是截取屏幕上可见的部分。

- **清空接收记录**：清除当前会话的接收报文。
- **清空全部记录**：清除当前会话的发送和接收报文，不是清除应用中所有连接的数据。
- 对需要保留的记录，先导出再清空。某些已封存的只读历史会话不能清空，界面会给出提示。

不要根据“分层存储”推断应用会定期删除数据库或无限期保留所有历史；当前没有可配置的自动保留周期。

## 10. 语言、主题与更新

在设置窗口的 **语言 / Language** 中选择“简体中文”或“English”，点击 **保存**立即应用，无需重启，也不会中断活动连接、监听或定时发送。取消不会保存语言选择；重置默认后仍需保存。

显示选项可调整深色主题、报文列表自动换行和等宽字体。界面语言不会翻译自定义配置名称或 TCP 载荷。

- **Windows/Linux 公开版**：通过“帮助 → 检查更新”手动检查最新稳定版；发现更新后前往发行页下载，不自动安装。
- **Mac App Store 版**：由 App Store 提供更新，不显示 GitHub 更新入口。
- 自行编译且没有注入发行版本时显示 `0.0.0-dev`，这不是正式版本号。

![中英文与深浅主题](https://tcp.onemoretry.cc/images/04-language-1.3.png)

## 11. 常见问题

| 现象 | 建议检查 |
| --- | --- |
| 连接被拒绝或超时 | 地址、端口、对端是否监听、网络及防火墙；客户端超时设置是否合适 |
| 服务端无法监听 | 地址是否属于当前电脑、端口是否被占用；优先用回环地址和空闲高位端口测试 |
| 本机能连，其他电脑不能连 | 是否只监听了 `127.0.0.1`；应改用局域网地址或适合的监听地址 |
| 已连接，但暂时没有报文 | 对端是否真正发送数据，分帧配置是否匹配，是否仍在等待完整消息 |
| 收发内容看起来不同 | 两端显示格式、协议编码、换行/分隔符是否一致；用 HEX 对照原始字节 |
| 一次发送拆成多条，或多次发送合并 | TCP 字节流行为；按协议启用正确的接收分帧 |
| 定时运行时手动发送不可用 | 先停止定时发送 |
| Mac 没有“检查更新” | 商店版由 App Store 更新，这是正常行为 |
| Linux 程序不能启动 | 检查执行权限、CPU 架构、系统兼容性及图形运行库 |

更多信息：[官方网站](https://tcp.onemoretry.cc/) · [隐私政策](https://tcp.onemoretry.cc/privacy.html) · [联系开发者](mailto:techforcc@gmail.com)
